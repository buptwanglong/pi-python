# basket-protocol

Gateway WebSocket wire protocol types for Basket: inbound (server → client) and outbound (client → server) message types, with `parse_inbound` and `serialize_outbound`.

Used by **basket-tui** and **basket-gateway** for strongly-typed WebSocket messaging.
