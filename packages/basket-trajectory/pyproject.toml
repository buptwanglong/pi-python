[tool.poetry]
name = "basket-trajectory"
version = "0.1.0"
description = "Task trajectory recording for agent runs (RL and tuning)"
authors = ["Pi Python Team"]
readme = "README.md"
packages = [{include = "basket_trajectory"}]
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

[tool.poetry.dependencies]
python = "^3.12"
pydantic = "^2.7"
basket-ai = {path = "../basket-ai", develop = true}
basket-agent = {path = "../basket-agent", develop = true}

[tool.poetry.group.dev.dependencies]
pytest = "^8.0.0"
pytest-asyncio = "^0.23.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
